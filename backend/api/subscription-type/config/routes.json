{
  "routes": [
    {
      "method": "GET",
      "path": "/subscription-types",
      "handler": "subscription-type.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/subscription-types/count",
      "handler": "subscription-type.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/subscription-types/:id",
      "handler": "subscription-type.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/subscription-types",
      "handler": "subscription-type.create",
      "config": {
        "policies": ["global::isAuthenticated"]
      }
    },
    {
      "method": "PUT",
      "path": "/subscription-types/:id",
      "handler": "subscription-type.update",
      "config": {
        "policies": ["global::isAuthenticated"]
      }
    },
    {
      "method": "DELETE",
      "path": "/subscription-types/:id",
      "handler": "subscription-type.delete",
      "config": {
        "policies": ["global::isAuthenticated"]
      }
    }
  ]
}
